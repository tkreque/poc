version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installation phase - $ENVIRONMENT"
      - sleep 15
  pre_build:
    commands:
      - echo "Pre-build phase"
       - sleep 15
  build:
    commands:
      - echo "Build phase"
      - sleep 30
  post_build:
    commands:
      - echo "Post-build phase"
      - sleep 15